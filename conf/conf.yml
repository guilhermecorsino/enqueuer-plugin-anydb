files:
  - examples/*

store:
  mongodb-connection-configuration:
    host: localhost
    port: 27017
    database: mydb
    username: root
    password: example
  postgres-connection-configuration:
    user: postgres
    password: postgres
    host: localhost
    port: 5432
    database: datastoreservicedb
  aws-dynamo-connection-configuration:
    endpoint: http://localhost:4566
    accessKeyId: na
    secretAccessKey: na
    region: us-east-1
