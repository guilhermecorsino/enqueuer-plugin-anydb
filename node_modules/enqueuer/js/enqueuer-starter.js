"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const logger_1 = require("./loggers/logger");
const enqueuer_runner_1 = require("./enqueuer-runner");
class EnqueuerStarter {
    constructor() {
        this.enqueuerRunner = new enqueuer_runner_1.EnqueuerRunner();
    }
    start() {
        return __awaiter(this, void 0, void 0, function* () {
            let statusCode = 1;
            try {
                const reports = yield this.enqueuerRunner.execute();
                statusCode = reports.every(report => report.valid) ? 0 : 1;
            }
            catch (error) {
                logger_1.Logger.fatal(`Execution error: ${error}`);
                statusCode = -1;
            }
            logger_1.Logger.info('Enqueuer execution is over (' + (statusCode === 0) + ')');
            logger_1.Logger.setLoggerLevel('info');
            logger_1.Logger.info(`Hope you had a great time`);
            return statusCode;
        });
    }
}
exports.EnqueuerStarter = EnqueuerStarter;
