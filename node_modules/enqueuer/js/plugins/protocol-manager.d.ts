import { PublisherModel } from '../models/inputs/publisher-model';
import { Publisher } from '../publishers/publisher';
import { SubscriptionModel } from '../models/inputs/subscription-model';
import { Subscription } from '../subscriptions/subscription';
import { Protocol } from '../protocols/protocol';
export declare class ProtocolManager {
    private protocols;
    createPublisher(publisherModel: PublisherModel): Publisher;
    createSubscription(subscriptionModel: SubscriptionModel): Subscription;
    addProtocol(protocol: Protocol): void;
    describeMatchingProtocols(description?: string): boolean;
    getProtocolsDescription(protocol?: string): {
        publishers: {}[];
        subscriptions: {}[];
    };
}
