import { HookModel } from '../models/outputs/hook-model';
export declare class HookReporter {
    private readonly hook;
    constructor(hookModel?: HookModel);
    addValues(hookModel?: HookModel): HookModel;
}
