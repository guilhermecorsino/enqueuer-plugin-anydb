export declare class StoreCodeGenerator {
    private readonly testsName;
    private readonly storeInstanceName;
    constructor(testsName: string, storeInstanceName: string);
    generate(store: any): string;
}
