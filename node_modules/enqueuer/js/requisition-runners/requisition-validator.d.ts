import { RequisitionModel } from '../models/inputs/requisition-model';
export declare class RequisitionValidator {
    validate(requisition: RequisitionModel): boolean;
    getErrorMessage(): string;
}
