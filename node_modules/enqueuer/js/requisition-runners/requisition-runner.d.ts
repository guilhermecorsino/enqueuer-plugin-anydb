import * as input from '../models/inputs/requisition-model';
import * as output from '../models/outputs/requisition-model';
export declare class RequisitionRunner {
    private requisition;
    private childrenRequisitionRunner;
    private requisitionReporter?;
    constructor(requisition: input.RequisitionModel);
    run(): Promise<output.RequisitionModel[]>;
    private iterateRequisition;
    private importRequisition;
    private interrupt;
    private emitNotification;
    private replaceVariables;
    private startRequisitionReporter;
    private timeoutPath;
    private happyPath;
    private executeChildren;
    private executeChild;
}
