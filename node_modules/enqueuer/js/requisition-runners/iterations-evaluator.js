"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const logger_1 = require("../loggers/logger");
class IterationsEvaluator {
    iterations(iterations) {
        if (iterations !== undefined) {
            try {
                //TODO be able to use 'requisition' as argument
                const evaluated = eval(iterations.toString());
                switch (typeof evaluated) {
                    case 'boolean':
                        return evaluated ? 1 : 0;
                    case 'number':
                    default:
                        return evaluated;
                }
            }
            catch (err) {
                logger_1.Logger.warning(`Error evaluating iterations: $${iterations}: ${err}`);
                return 0;
            }
        }
        return 1;
    }
}
exports.IterationsEvaluator = IterationsEvaluator;
