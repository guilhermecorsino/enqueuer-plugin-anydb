"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const requisition_parser_1 = require("./requisition-parser");
const fs = __importStar(require("fs"));
class RequisitionFileParser {
    parseFile(filename) {
        const fileBufferContent = fs.readFileSync(filename).toString();
        const requisition = new requisition_parser_1.RequisitionParser().parse(fileBufferContent);
        requisition.name = requisition.name || filename;
        return requisition;
    }
}
exports.RequisitionFileParser = RequisitionFileParser;
