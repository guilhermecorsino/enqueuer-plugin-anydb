export declare enum Notifications {
    REQUISITION_RAN = 0
}
export declare class NotificationEmitter {
    private static readonly notificationEmitter;
    private readonly eventEmitter;
    private constructor();
    static on(event: Notifications, listener: (...args: any[]) => void): NotificationEmitter;
    static emit(event: Notifications, ...args: any[]): NotificationEmitter;
}
