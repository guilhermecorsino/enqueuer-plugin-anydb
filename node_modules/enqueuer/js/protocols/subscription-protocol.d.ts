import { Protocol } from './protocol';
import { SubscriptionModel } from '../models/inputs/subscription-model';
import { Subscription } from '../subscriptions/subscription';
import { ProtocolDocumentation } from './protocol-documentation';
export declare class SubscriptionProtocol extends Protocol {
    private readonly createFunction;
    constructor(name: string, createFunction: (subscriptionModel: SubscriptionModel) => Subscription, documentation?: ProtocolDocumentation);
    create(subscription: SubscriptionModel): Subscription;
}
