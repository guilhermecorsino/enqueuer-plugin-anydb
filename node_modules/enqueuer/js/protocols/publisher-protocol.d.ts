import { Protocol } from './protocol';
import { PublisherModel } from '../models/inputs/publisher-model';
import { Publisher } from '../publishers/publisher';
import { ProtocolDocumentation } from './protocol-documentation';
export declare class PublisherProtocol extends Protocol {
    private readonly createFunction;
    constructor(name: string, createFunction: (publisherModel: PublisherModel) => Publisher, documentation?: ProtocolDocumentation);
    create(publisher: PublisherModel): Publisher;
}
